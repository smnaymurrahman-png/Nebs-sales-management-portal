{
  "name": "nebs-seller-portal",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "migrate": "cd backend && npm run migrate",
    "seed": "cd backend && npm run seed"
  }
}
